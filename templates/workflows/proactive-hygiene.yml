name: Proactive Hygiene

on:
  schedule:
    - cron: '17 4 * * *'   # adjust schedule as needed
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: proactive-hygiene
  cancel-in-progress: false

jobs:
  hygiene:
    uses: gumfactor/optimised_agentic_workflow/.github/workflows/proactive-hygiene-reusable.yml@main
