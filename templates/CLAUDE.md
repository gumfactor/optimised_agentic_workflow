# Compatibility Alias

This file exists for tool compatibility.

Canonical project instructions live in `AGENTS.md`.

If your tool only reads `CLAUDE.md`, treat this file as an alias and load `AGENTS.md` first.
