name: Policy Compliance (Reusable)

on:
  workflow_call:

jobs:
  policy-compliance:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run policy compliance lint
        run: bash scripts/policy-lint.sh
